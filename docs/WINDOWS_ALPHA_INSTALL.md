# Installing the Windows alpha

Download the `.exe` attached to the tagged Haunted Toaster GitHub prerelease.

The current alpha is unsigned. Windows may show a SmartScreen warning; use only builds downloaded from this repository's GitHub Release page and verify the release/tag you intended to install.

The release also publishes a portable executable when produced by electron-builder, so installation is not required for that variant.
