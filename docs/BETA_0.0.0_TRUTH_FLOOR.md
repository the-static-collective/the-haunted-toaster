# Haunted Toaster — BETA 0.0.0 Truth Floor

Status: **release-cut truth** for draft PR #275 on `beta/reconcile-0.0.0`.

`main` remains product authority until #275 lands. This file describes the proposed BETA 0.0.0 carrier and must not be read as a claim that an unmerged branch is already canonical.

## Identity

- Human release epoch: **HAUNTED TOASTER — BETA 0.0.0**
- Application package identity: **`0.0.0-beta.0`**
- Reconciliation carrier: **PR #275 — `beta/reconcile-0.0.0`**
- Superseded duplicate carrier: **PR #274 — provenance only; do not merge**
- Governing issue: **#273**

## Frozen scope

BETA 0.0.0 is a reconciliation cut, not a feature expansion cut.

The following are explicitly **not** BETA 0.0.0 blockers and must not be pulled into this release merely because adjacent substrate exists:

- motion-mask work;
- temporal Video modes;
- Aperture Shape;
- HyperFood / HyperFrames descendants;
- broader cross-session taste-learning or preference models;
- new creative verbs or topology families that are not already wired on this carrier.

## Active / product-wired truth

The current #275 carrier proves the following ordinary path:

```text
song + optional admitted material + lyrics
        ↓
analysis / Starting Field
        ↓
Toast Feels + latent Toastmood ecology
        ↓
deterministic six-up creatures
        ↓
focus = observation only
        ↓
KEEP or SCRAPE
        ↓
KEEP-authorized ResolvedTimeline
        ↓
preview / final render
        ↓
video + score + timeline + Dogram + Haunted Haiku + receipt sidecars
```

Current authority law:

```text
focus != verdict
focus != ancestry
KEEP = explicit continuation permission
SCRAPE = reject this family and search again
SCRAPE != negative feature preference
render(candidate) requires KEEP in the ordinary candidate path
TEST 6 forced witness remains a distinct declared exception
```

More specifically:

- `session.keep()` exists and grants continuation-only ancestry to the exact kept creature.
- `session.scrape()` exists and deterministically escalates search without granting ancestry or inferring a scalar dislike model.
- Ordinary focus is observational; it does not silently admit ancestry.
- The ordinary candidate render handoff refuses an unkept candidate.
- Dogram evidence remains `comparison-only` and does not decide meaning or render authority.
- Haunted Haiku remains deterministic `descriptive-only` publication copy derived from completed receipt evidence.
- `VisualScore → ResolvedTimeline → preview/final render → sidecars → receipt` remains the accepted execution chain.

## Executable substrate / expert surface

The carrier also contains deeper machinery that is real but is **not an ordinary-user release gate**:

- MUTATE / CROSS / STOMP / CONVERGE;
- locks, genealogy, branch/frontier exploration, and explicit candidate operations;
- topology, listening, foreign-material, typography, Listener, and evidence machinery already present on the carrier;
- development and witness controls that expose more of the genome than the intended appliance surface.

Their existence does not require a human to manually summon every internal verb before BETA 0.0.0 can be cut.

## Ancestry / superseded truth

PR #274 and PR #275 diverged from the same WALK-era ancestry. #274 retained an intentional RED continuation contract: its candidate session did **not** implement `keep()` / `scrape()`. #275 carries the actual continuation implementation and the Dogram/Haiku proof as focused current tests.

Therefore:

- #274 is retained as provenance, not as a competing BETA 0.0.0 reality;
- no #274 executable behavior needs to be merged over #275;
- historical `0.5.0-alpha.*` package identities remain historical evidence and are not rewritten;
- scalar preference semantics do not regain ambient authority through focus or SCRAPE.

## Release gate

BETA 0.0.0 is solid only when all of these refer to the **same exact head**:

1. locked install + consolidated full suite;
2. candidate/render smoke, including KEEP → render;
3. production dependency audit;
4. production browser/UI witness;
5. Dogram + Haunted Haiku receipt composition;
6. Windows Setup + Portable package from that exact head;
7. recorded head SHA, tree, workflow/run, artifact digest, and Build Info;
8. one normal packaged use: **song in → six → KEEP/SCRAPE → finished video**;
9. no concrete blocker discovered by that use;
10. explicit exact-head landing approval before merge.

Machine proof may establish machine claims. The final packaged-use witness is intentionally ordinary use, not a ceremony of manually forcing every internal operation.
