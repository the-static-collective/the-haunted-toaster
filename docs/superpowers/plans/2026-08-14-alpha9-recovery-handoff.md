# Haunted Toaster alpha.9 recovery handoff

## Recovery boundary

Issue #127 remains the implementation authority. This branch reconstructs the stranded local alpha.9 work from current `main` without importing a divergent historical branch.

Recovered execution boundary:

- `toaster-raster-4` / `visual-language-v3` is the explicit opt-in generation;
- Mutation Lattice v1 binds six layers: Skeleton, Body, Frame, Skin, Weather, Time;
- ordinary Toast Feels retain alpha.8 numeric pressure and add sparse six-layer categorical affinity;
- Shape Pack v1 adds `elastic-spine`, `split-horizon`, `cathedral-fan`, and `echo-tunnel` without changing ancestral topology identifiers;
- Topology Arc v1 records zero to two bounded apparition windows with `dissolve | scar | succession` outcomes;
- topology locks prohibit topology arcs absolutely;
- candidate family type is derived from authoritative family evidence rather than caller labels;
- recorded lattice plans/refusals are exact-key and hash validated before replay or redecoration;
- CONVERGE provenance is checked against its recorded frontier/derived-seed evidence when complete provenance is available.

## TDD recovery witness

RED head `07f79dfbefb5eca1dc467eca09fd2ca629378ed8` ran the existing suite with the new alpha.9 contract absent: 277 existing tests passed and exactly the new contract failed because the Mutation Lattice implementation did not exist.

A disposable recovery checkout then applied the reconstructed execution patch and passed:

- focused `alpha9-mutation-lattice.test.cjs`;
- source-wide `npm --prefix src/full-measure run check`.

The exact witnessed modified tree was preserved as workflow artifact `alpha9-recovery-tree` from run `31844778951` before being committed to the branch.

## Current gate

This handoff intentionally does **not** claim alpha.9 is test-ready yet. The branch must still pass the repository's ordinary `npm run verify`, runtime dependency audit, browser witness, production FFmpeg smoke, and Windows Setup/Portable package witness required by issue #127. Any alpha.8 version/capability assertions that fail because the active appliance has deliberately moved to raster-4 must be advanced narrowly rather than weakening historical compatibility tests.
