# BETA 0.0.0 Reconciliation Implementation Plan

Status: approved / executing
Date: 2026-09-15
Issue: #273
Working branch: `beta/reconcile-0.0.0`
Starting carrier: `walk/e-sequential-braid`
Starting WALK head: `ddbe15975d99dded7b755d5138e1a7cc2da29f84`
Starting WALK tree: `02a250aaea878a220297e02e86db6a087d754abd`
Compared `main`: `3c6fd48c6d8a26bcccf1f2d96f6f3c609e52dac8`
Merge base: `10f65ca1f6d721178355c5c67b933e0183753c45`

## Goal

Create the first coherent present-tense Haunted Toaster without manufacturing a new feature lane.

> Plug the proven wires together. Let errors become evidence. Let receipts speak over time.

BETA 0.0.0 means that the scattered executable machine has one proposed carrier whose product loop, package identity, receipts, and repository truth agree. `main` remains product authority until an explicit landing decision.

## Non-goals

- no blind merge of WALK into `main` or `main` into WALK;
- no tag, release, publish, or promotion without separate approval;
- no attempt to finish the entire roadmap;
- no requirement to manufacture human witness for internal verbs the ordinary UI cannot deliberately summon;
- no new renderer authority for Dogram or Haunted Haiku;
- no #223 Aperture Shape implementation in 0.0.0;
- no full cross-session taste-learning or preference model.

## Task 1 — Executable-truth archaeology

Classify current issues/PRs against code + tests + current carrier before issue prose.

Immediate known dispositions:

- #147 candidate ecology / exact two-parent CROSS — LANDED via #179 and current WALK;
- #206 deterministic TEST 6 — LANDED in current WALK;
- #242 Stage A RESPONSE × SCOPE × CONSEQUENCE — Stage A LANDED via #251/#256/current WALK; retain only genuinely later work;
- #44 founding foreign-video crossing — LANDED/SUPERSEDED by current Video/VSPantry + #250/#262/#263 descendants;
- #225 L BRANCH — current WALK contains Lane Bank, Mix Plan, execution, receipt, integrity and production tests; verify no unresolved acceptance cell, then disposition;
- #195 topology events — current WALK contains APERTURE/SPEAK/GRAB/GROW/BODY event composition and proof; separate future calibration/shape work rather than keeping the founding issue open;
- #197 Resolution Field founding bounded lane — current WALK contains the bounded Resolution/Atmosphere machinery and witness lineage; preserve richer future research separately;
- #223 Aperture Shape — NOT-BUILT, keep open as post-beta frontier.

Do not close any issue merely because its title resembles current code. Verify the exact contract and descendants first.

## Task 2 — Re-port main-only Dogram fruit

Files:

- add `src/full-measure/src/render/dogram-trace.cjs` from current `main` semantics;
- add `src/full-measure/tests/dogram-trace.test.cjs`;
- add `src/full-measure/tests/dogram-sidecar-write.test.cjs`;
- merge Dogram sidecar behavior into current WALK `src/full-measure/src/render/receipt.cjs` without dropping topology/L BRANCH/Stage-A/genealogy promotion.

Required law:

- Dogram sidecar is `comparison-only`;
- incomplete evidence receipts do not emit Dogram;
- stale Dogram sidecar is removed for incomplete receipts;
- receipt write failure removes any just-written Dogram sidecar;
- Dogram never acquires render, artistic, causal, or historical authority.

Verification:

- Dogram focused tests;
- receipt tests;
- consolidated application proof.

## Task 3 — Re-port Haunted Haiku fruit

Donor: open PR #268 / `feat/haunted-haiku-receipts`.

Files:

- add `src/full-measure/src/renderer/haunted-haiku-ui.js`;
- add `src/full-measure/tests/haunted-haiku-receipt.test.cjs`;
- add `src/full-measure/tests/haunted-haiku-ui.test.cjs`;
- semantically re-port preload hook into current WALK preload;
- semantically re-port publication receipt decoration into the already-reconciled WALK+Dogram receipt writer.

Required law:

- generated only after an accepted completed render;
- deterministic from retained receipt identity;
- `authority: descriptive-only`;
- no new IPC or network dependency;
- failed/unaccepted renders receive no publication copy;
- publication text never changes accepted score/timeline/render authority.

Verification:

- Haiku focused tests;
- browser witness;
- packaged witness because preload/UI is touched.

## Task 4 — Continuation Without Preference vertical slice

Design authority:

`docs/superpowers/specs/2026-09-15-madd-clown-crazy-slots-design.md`

TDD contract first.

Backend behavior:

1. candidate focus/inspection is observational and does not enter accepted history;
2. explicit KEEP grants continuation permission to one exact current candidate;
3. ordinary production render requires KEEP (forced TEST 6 witness remains separately lawful);
4. SCRAPE rejects the current family as direct continuation without adding negative feature weights;
5. a deterministic internal dealer chooses the next existing lawful search operation;
6. machine-selected search ancestry is evidence, not human acceptance;
7. KEEP/SCRAPE/dealer decisions leave compact deterministic receipts;
8. receipt/replay paths preserve exact family, candidate, operation, policy and resulting family identities;
9. old scalar preference semantics acquire no ambient authority.

UI behavior:

- clicking a creature = focus only;
- ordinary primary judgment = KEEP / SCRAPE;
- manual move deck and locks remain available as explicit expert/debug tooling during migration, not required ordinary controls;
- KEEP binds the exact candidate for final render;
- SCRAPE asks the Toaster for another lawful family and surfaces the compact dealer receipt.

Verification:

- RED tests before implementation;
- focused candidate-session tests;
- replay/authority tests;
- candidate UI/browser witness;
- full repository proof.

## Task 5 — Truth floor and release identity

Update the proposed carrier only after executable composition is green:

- root `README.md`;
- `src/full-measure/README.md`;
- `CURRENT_SPINE.md`;
- add one compact BETA truth-floor manifest classifying ACTIVE/product-wired, executable substrate, ancestry-only, and not-built;
- preserve Starting Field → Toast Feels → latent Toastmood ecology → creatures-as-interface ancestry;
- document superseded scalar preference semantics;
- ensure Build Info/package identity names the coherent carrier;
- choose semver-valid beta package identity without rewriting historical alpha artifact identity.

## Task 6 — BETA 0.0.1 frontier registry

Create one post-beta umbrella after the archaeology pass. It must gather every still-live item without promoting all of them to equal release-blocker status.

Use four lanes:

1. `READY / EXECUTABLE` — bounded post-beta slices with current contracts;
2. `PARTIAL / DESCENDANT` — substantial substrate exists but a specific next proof remains;
3. `RESEARCH / PROBE` — mathals, speculative experiments, controlled witnesses;
4. `PARKED / LATER` — legitimate work intentionally outside the next product cut.

At minimum re-evaluate for the registry:

- #223 Aperture Shape;
- Listener vNext / Opening Backfill / placement controls;
- cross-session Toaster Memory / Re-toast / Thoughtline;
- Sigil Language / Sigil Algebra completion;
- deeper foreign-material metabolism / attributable future-pressure;
- MOLT / HAUNT productization;
- Native Anatomy / Resonant Disturbance coupling;
- Witness Session;
- Windows FFmpeg `0xC0000005` root-cause investigation;
- YouTube publication flow beyond descriptive Haunted Haiku copy;
- Specimen Pulse / receipt-derived scar field;
- current research issues such as MANDELBROT, COLLISION-AWARE-WITNESS, CONTROLLED-DESCENDANT, CREATIVE CONTRABAND;
- HyperFood #269/#270;
- any other still-open issue whose exact contract survives executable-truth classification.

0.0.1 must be an anti-loss registry, not a promise to finish every lane in one release.

## Task 7 — Final proof and proposed landing

Run fresh proof on the exact reconciliation head:

```text
npm --prefix src/full-measure ci
npm run verify
npm --prefix src/full-measure test
npm --prefix src/full-measure run smoke
npm --prefix src/full-measure run pack
npm --prefix src/full-measure audit --omit=dev --audit-level=high
```

Run canonical renderer witness and browser/UI witness through repository-owned workflows.

Because preload/UI/package identity changes are expected, build Windows Setup + Portable from the exact selected head and record:

- source head SHA;
- tree SHA;
- workflow/run identity;
- merge-ref/tree relationship when applicable;
- artifact name/id/size/digest;
- Build Info identity.

Then use the packaged Toaster normally. Treat reproducible runtime RED as the next bounded integration repair rather than reopening speculative matrix archaeology.

Open a reconciliation PR to `main`, but do not merge it without explicit per-PR human landing approval.

## Stop condition

Stop BETA 0.0.0 reconciliation when:

> The scattered machine has become one truthful proposed machine, its ordinary KEEP/SCRAPE loop is executable, its receipts name what happened without acquiring authority they do not own, and the remaining work has one honest 0.0.1 frontier registry.
