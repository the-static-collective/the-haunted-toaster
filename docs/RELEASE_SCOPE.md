# Release scope

The release workflow publishes only artifacts already produced by the verified Haunted Toaster build. It does not alter candidate generation, resolution, preview semantics, rendering authority, or receipts.

Publishing is a transport step after proof, not a new execution authority.
