# Release assets

Tagged Windows prereleases attach the installer and portable `.exe` outputs generated by electron-builder. Workflow `.yml` metadata remains available as a workflow artifact when produced, but the user-facing release surface is the Windows executable set.
