# the-haunted-toaster
SONG IN → FINISHED VIDEO OUT


On Windows: unzip it and double-click START_FULL_MEASURE.bat. First setup needs Node 22+ and internet once; rendering afterward is local and offline.

It already has:

MP3/WAV → complete 1080p MP4
optional image and lyrics
Porchlight, Wire Orchard, and Absolute Residual garments
audio-reactive motion and section-aware changes
progress and safe cancellation
original MP3 stream-copy where possible
cryptographic Video Receipt with hashes and duration proof
Windows installer/portable-build workflow

Proof passed:

WAV render: exact 12.000 seconds, 0 ms delta
MP3 + image: original stream copied, 24 ms mux delta
7/7 tests passing

Honest alpha boundary: lyric timing is evenly distributed, not word-aligned yet. This is source alpha rather than a signed installer.
