{
  "name": "haunted-toaster-repository",
  "private": true,
  "description": "Repository command facade; src/full-measure/package.json is the application manifest and version authority.",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "verify": "npm --prefix src/full-measure run check && npm --prefix src/full-measure test && npm --prefix src/full-measure run smoke",
    "start": "npm --prefix src/full-measure start",
    "dist:win": "npm --prefix src/full-measure run dist -- --win"
  }
}
